<UserControl x:Class="AdbSync.App.Controls.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
             Height="36" Background="Transparent">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="14,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
            <Border Width="13" Height="13" CornerRadius="3" Margin="0,0,8,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#7C5CFF" Offset="0"/>
                        <GradientStop Color="#4C6FFF" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>
            <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                       FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Brush.Text.Primary}" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel x:Name="ButtonsPanel" Grid.Column="2" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
            <Button x:Name="MinimizeButton" Style="{StaticResource CaptionButtonBase}" Click="Minimize_Click" ToolTip="Minimize">
                <Path Data="M0,0 L10,0" Stroke="{StaticResource Brush.Text.Secondary}" StrokeThickness="1"/>
            </Button>
            <Button x:Name="MaximizeButton" Style="{StaticResource CaptionButtonBase}" Click="Maximize_Click" ToolTip="Maximize">
                <Path x:Name="MaximizeIcon" Stroke="{StaticResource Brush.Text.Secondary}" StrokeThickness="1">
                    <Path.Style>
                        <Style TargetType="Path">
                            <Setter Property="Data" Value="M0.5,0.5 H9.5 V9.5 H0.5 Z"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}" Value="Maximized">
                                    <Setter Property="Data" Value="M0.5,2.5 H7.5 V9.5 H0.5 Z M2.5,2.5 V0.5 H9.5 V7.5 H7.5"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Path.Style>
                </Path>
            </Button>
            <Button Style="{StaticResource CloseCaptionButton}" Click="Close_Click" ToolTip="Close">
                <Path Data="M0,0 L10,10 M0,10 L10,0" Stroke="{StaticResource Brush.Text.Secondary}" StrokeThickness="1"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
