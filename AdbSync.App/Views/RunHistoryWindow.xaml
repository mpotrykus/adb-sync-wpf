<Window x:Class="AdbSync.App.Views.RunHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:interop="clr-namespace:AdbSync.App.Interop"
        xmlns:controls="clr-namespace:AdbSync.App.Controls"
        xmlns:conv="clr-namespace:AdbSync.App.Converters"
        interop:AcrylicEffect.Enabled="True"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Title="Run History" Icon="/Assets/app.ico" Height="820" Width="1080" MinHeight="660" MinWidth="820" WindowStartupLocation="CenterOwner">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="8" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <conv:RunOutcomeToBrushConverter x:Key="RunOutcomeToBrushConverter"/>
        <Style x:Key="StatCard" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
        </Style>
        <Style x:Key="StatLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Secondary}"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Small, Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="StatValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style x:Key="ChartCard" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
        <Style x:Key="LegendDot" TargetType="Ellipse">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="Margin" Value="0,0,7,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="LegendLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Secondary}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Border Style="{StaticResource WindowRootBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <controls:TitleBar Grid.Row="0"/>

            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="188"/>
                    <RowDefinition Height="*" MinHeight="160"/>
                    <RowDefinition Height="150"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Summary stat tiles -->
                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource StatCard}">
                        <StackPanel>
                            <TextBlock Text="TOTAL RUNS" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="TotalRunsValue" Style="{StaticResource StatValue}"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource StatCard}">
                        <StackPanel>
                            <TextBlock Text="SUCCESS RATE" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="SuccessRateValue" Style="{StaticResource StatValue}"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="2" Style="{StaticResource StatCard}">
                        <StackPanel>
                            <TextBlock Text="AVG DURATION" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="AvgDurationValue" Style="{StaticResource StatValue}"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="3" Style="{StaticResource StatCard}">
                        <StackPanel>
                            <TextBlock Text="DATA TRANSFERRED" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="DataTransferredValue" Style="{StaticResource StatValue}"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="4" Style="{StaticResource StatCard}" Margin="0">
                        <StackPanel>
                            <TextBlock Text="ERRORS" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="ErrorsValue" Style="{StaticResource StatValue}"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Charts -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource ChartCard}" Margin="0,0,12,16">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Text="Duration per run" Style="{StaticResource SectionLabel}"/>
                            <controls:TrendBarChart x:Name="DurationChart"/>
                        </DockPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource ChartCard}">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Text="Outcome breakdown" Style="{StaticResource SectionLabel}"/>
                            <StackPanel x:Name="OutcomeLegend" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="12,0,0,0"/>
                            <controls:OutcomeDonutChart x:Name="OutcomeChart"/>
                        </DockPanel>
                    </Border>
                </Grid>

                <DataGrid x:Name="RunsGrid" Grid.Row="2" Margin="0,0,0,12" AutoGenerateColumns="False" IsReadOnly="True"
                          CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="None" RowHeight="40"
                          SelectionChanged="RunsGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="STARTED" Binding="{Binding StartedAtText}" Width="150"/>
                        <DataGridTextColumn Header="DURATION" Binding="{Binding DurationText}" Width="90"/>
                        <DataGridTextColumn Header="PULL" Binding="{Binding PullDurationText}" Width="60"/>
                        <DataGridTextColumn Header="PUSH" Binding="{Binding PushDurationText}" Width="60"/>
                        <DataGridTextColumn Header="FILES" Binding="{Binding FilesText}" Width="165"/>
                        <DataGridTextColumn Header="SIZE" Binding="{Binding SizeText}" Width="90"/>
                        <DataGridTextColumn Header="ERRORS" Binding="{Binding ErrorCount}" Width="80"/>
                        <DataGridTemplateColumn Header="OUTCOME" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Style="{StaticResource StatusDot}" Fill="{Binding Outcome, Converter={StaticResource RunOutcomeToBrushConverter}}"/>
                                        <TextBlock Text="{Binding OutcomeText}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                                                   Foreground="{Binding Outcome, Converter={StaticResource RunOutcomeToBrushConverter}}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="ERROR" Binding="{Binding ErrorMessage}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    <Setter Property="Foreground" Value="{StaticResource Brush.Danger}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBox x:Name="LogTextBox" Grid.Row="3" IsReadOnly="True" TextWrapping="NoWrap" FontFamily="Consolas"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         Text="Select a run above to view its log."/>

                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="Close" Width="80" Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
