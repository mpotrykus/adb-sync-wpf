<Window x:Class="AdbSync.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:interop="clr-namespace:AdbSync.App.Interop"
        xmlns:controls="clr-namespace:AdbSync.App.Controls"
        interop:AcrylicEffect.Enabled="True"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Title="Settings" Icon="/Assets/app.ico" SizeToContent="Height" Width="520" MaxHeight="820"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="0" GlassFrameThickness="0" CornerRadius="8" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border Style="{StaticResource WindowRootBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <controls:TitleBar Grid.Row="0" ShowMinMax="False"/>

            <ScrollViewer Grid.Row="1" Margin="20" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <Border Style="{StaticResource CardBorder}">
                        <StackPanel>
                            <TextBlock Text="Projects Directory" Style="{StaticResource SectionLabel}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="ProjectsDirectoryBox" Grid.Column="0" Margin="0,0,8,0"
                                         controls:TextBoxHelper.Placeholder="C:\Users\you\Documents\AdbSync Projects"/>
                                <Button Grid.Column="1" Content="Browse..." Click="Browse_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardBorder}">
                        <StackPanel>
                            <TextBlock Text="Notifications" Style="{StaticResource SectionLabel}"/>
                            <CheckBox x:Name="StartAtLoginBox" Content="Start AdbSync when I log in" Margin="0,0,0,10"/>
                            <CheckBox x:Name="ShowInfoNotificationsBox" Content="Show notifications for successful syncs" Margin="0,0,0,10"/>
                            <CheckBox x:Name="ShowErrorNotificationsBox" Content="Show notifications for sync errors"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardBorder}">
                        <StackPanel>
                            <TextBlock Text="Quiet Hours" Style="{StaticResource SectionLabel}"/>
                            <CheckBox x:Name="QuietHoursEnabledBox" Content="Suppress notifications during quiet hours" Margin="0,0,0,10"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="90"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="From" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBox Grid.Column="1" x:Name="QuietHoursStartBox" controls:TextBoxHelper.Placeholder="22:00"/>
                                <TextBlock Grid.Column="3" Text="To" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBox Grid.Column="4" x:Name="QuietHoursEndBox" controls:TextBoxHelper.Placeholder="07:00"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardBorder}">
                        <Expander Header="Advanced" IsExpanded="False">
                            <StackPanel>
                                <TextBlock Text="LOCKING &amp; CONCURRENCY" Style="{StaticResource SubSectionLabel}" Margin="0,4,0,8"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="90"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Stale lock timeout (hours)" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="StaleLockHoursBox" controls:TextBoxHelper.Placeholder="4"/>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Max concurrent jobs" VerticalAlignment="Center" Margin="0,10,0,0"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="MaxConcurrentJobsBox" Margin="0,10,0,0" controls:TextBoxHelper.Placeholder="1"/>
                                </Grid>

                                <Border Height="1" Background="{StaticResource Brush.Divider}" Margin="0,16,0,14"/>

                                <TextBlock Text="RETENTION" Style="{StaticResource SubSectionLabel}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="90"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Log retention (days)" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="LogRetentionDaysBox" controls:TextBoxHelper.Placeholder="30"/>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Max log file size (MB)" VerticalAlignment="Center" Margin="0,10,0,0"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="PerLogFileMaxMegabytesBox" Margin="0,10,0,0" controls:TextBoxHelper.Placeholder="5"/>
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Conflict backup retention (days)" VerticalAlignment="Center" Margin="0,10,0,0"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="ConflictRetentionDaysBox" Margin="0,10,0,0" controls:TextBoxHelper.Placeholder="30"/>
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Checkpoint retention (days)" VerticalAlignment="Center" Margin="0,10,0,0"/>
                                    <TextBox Grid.Row="3" Grid.Column="1" x:Name="CheckpointRetentionDaysBox" Margin="0,10,0,0" controls:TextBoxHelper.Placeholder="30"/>
                                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Run history entries kept per job" VerticalAlignment="Center" Margin="0,10,0,0"/>
                                    <TextBox Grid.Row="4" Grid.Column="1" x:Name="MaxRunHistoryEntriesBox" Margin="0,10,0,0" controls:TextBoxHelper.Placeholder="50"/>
                                </Grid>

                                <Border Height="1" Background="{StaticResource Brush.Divider}" Margin="0,16,0,14"/>

                                <TextBlock Text="TRANSFER" Style="{StaticResource SubSectionLabel}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="90"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Bandwidth throttle (KB/s, blank = unlimited)" VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="BandwidthThrottleKBpsBox" controls:TextBoxHelper.Placeholder="unlimited"/>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Transfer retry attempts" VerticalAlignment="Center" Margin="0,10,0,0"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="RetryMaxAttemptsBox" Margin="0,10,0,0" controls:TextBoxHelper.Placeholder="3"/>
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Retry backoff (seconds)" VerticalAlignment="Center" Margin="0,10,0,0"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="RetryBackoffSecondsBox" Margin="0,10,0,0" controls:TextBoxHelper.Placeholder="5"/>
                                </Grid>

                                <Border Height="1" Background="{StaticResource Brush.Divider}" Margin="0,16,0,14"/>

                                <TextBlock Text="SAFETY &amp; CONFLICTS" Style="{StaticResource SubSectionLabel}"/>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="90"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Push safety minimum (% of baseline)" VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="1" x:Name="PushSafetyMinimumPercentBox" controls:TextBoxHelper.Placeholder="25"/>
                                </Grid>
                                <CheckBox x:Name="BackupConflictLosersBox" Content="Back up the losing side of a merge conflict before overwriting it"/>
                            </StackPanel>
                        </Expander>
                    </Border>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="Cancel_Click" IsCancel="True"/>
                        <Button Content="Save" Width="90" Style="{StaticResource PrimaryButton}" Click="Save_Click" IsDefault="True"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
