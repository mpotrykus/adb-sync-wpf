<Window x:Class="AdbSync.App.Views.DeviceEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:interop="clr-namespace:AdbSync.App.Interop"
        xmlns:controls="clr-namespace:AdbSync.App.Controls"
        interop:AcrylicEffect.Enabled="True"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Title="Devices" Height="460" Width="600" MinHeight="360" MinWidth="480" WindowStartupLocation="CenterOwner">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="8" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border Style="{StaticResource WindowRootBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <controls:TitleBar Grid.Row="0"/>

            <Grid Grid.Row="1" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListBox x:Name="DeviceList" Grid.Row="0" Grid.Column="0" SelectionChanged="DeviceList_SelectionChanged"/>

                <Border Grid.Row="0" Grid.Column="2" Style="{StaticResource CardBorder}" Margin="0">
                    <StackPanel>
                        <TextBlock Text="Name" Style="{StaticResource SectionLabel}"/>
                        <TextBox x:Name="NameBox" Margin="0,0,0,14"/>

                        <TextBlock Text="Connection" Style="{StaticResource SectionLabel}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <RadioButton x:Name="WifiRadio" Content="WiFi (IP)" GroupName="ConnType" Margin="0,0,20,0" IsChecked="True"/>
                            <RadioButton x:Name="UsbRadio" Content="USB (serial)" GroupName="ConnType"/>
                        </StackPanel>
                        <TextBox x:Name="IpOrSerialBox" Margin="0,0,0,14"/>

                        <Button Content="Test Connection" Click="TestConnection_Click" HorizontalAlignment="Left"/>
                        <TextBlock x:Name="TestResultText" Margin="0,10,0,0" TextWrapping="Wrap" Style="{StaticResource CaptionText}"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,16,0,0" HorizontalAlignment="Right">
                    <Button Content="New" Width="80" Margin="0,0,8,0" Click="New_Click"/>
                    <Button Content="Delete" Width="80" Margin="0,0,20,0" Style="{StaticResource DangerButton}" Click="Delete_Click"/>
                    <Button Content="Close" Width="80" Margin="0,0,8,0" Click="Close_Click"/>
                    <Button Content="Save" Width="80" Style="{StaticResource PrimaryButton}" Click="SaveDevice_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
